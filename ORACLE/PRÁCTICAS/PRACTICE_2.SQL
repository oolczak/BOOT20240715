SELECT LAST_NAME, SALARY AS SAL FROM EMPLOYEES;
    -- TRUE
    -- EL ALIAS SI ES UNA PALABRA SOLA PUEDE IR SIN COMILLAS

SELECT * FROM JOB_GRADES;
    -- TRUE

SELECT EMPLOYEE_ID, LAST_NAME SAL X 12 ANUAL SALARY FROM EMPLOYEES;
    -- LOS ERRORES SON:
    -- FALTA DE COMA DESPUÉS DE LAST NAME
    -- SALARY NO PUEDE APARECER CON UN ALIAS NO ESTABLECIDO
    -- LA MULTIPLICACIÓN ES CON 'X' NO CON '*'
    -- RESULTADO: SELECT EMPLOYEE_ID, LAST_NAME, 12*SALARY AS ANUAL_SALARY FROM EMPLOYEES;

-- 4. DEPARTMENTS TABLE
    DESC DEPARTMENTS;
    SELECT DEPARTMENT_ID, DEPARTMENT_NAME, MANAGER_ID, LOCATION_ID FROM DEPARTMENTS;

-- 5. EMPLOYEES TABLE
-- A.
    DESC EMPLOYEES;

-- B. 
    SELECT EMPLOYEE_ID, LAST_NAME, JOB_ID, HIRE_DATE AS STARTDATE FROM EMPLOYEES;

-- 6. UNIQUE IDS
    SELECT UNIQUE JOB_ID FROM EMPLOYEES;

-- 7. HR DEPARTMENT
    SELECT EMPLOYEE_ID AS "EMP #", LAST_NAME AS EMPLOYEE, JOB_ID AS JOB, HIRE_DATE AS "HIRE DATE" FROM EMPLOYEES;

-- 8. EMPLOYEE AND TITLE
    SELECT LAST_NAME || ', ' || JOB_ID AS "EMPLOYEE AND TITLE" FROM EMPLOYEES;

-- 9. FINAL EXERCISE
    SELECT EMPLOYEE_ID || ', ' || FIRST_NAME|| ', ' || LAST_NAME || ', ' || PHONE_NUMBER || ', ' || JOB_ID || ', ' || HIRE_DATE || ', ' || SALARY || ','  ||DEPARTMENT_ID AS THE_OUTPUT FROM EMPLOYEES;
